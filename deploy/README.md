# Deployment Assets

This directory is populated during CI/CD.

- `current-taskdef.json` is fetched from ECS.
- `taskdef.candidate.json` is the task definition revision with the new image tag.
- `appspec.json` is generated by `scripts/render_appspec.py` and uploaded to S3 for CodeDeploy.
