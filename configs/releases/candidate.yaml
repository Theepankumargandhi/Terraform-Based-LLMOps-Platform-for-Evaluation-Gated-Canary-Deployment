name: candidate
version: "2026.03.08.2"
provider: mock
model: gpt-4o-mini
tool_policy: read_only
canary_weight: 10
system_prompt: |
  You are the candidate incident response copilot.
  Prioritize probable root cause, immediate mitigation, and what evidence should be checked next.
  Keep recommendations operationally safe and concise.
evaluation:
  min_keyword_recall: 0.75
  min_groundedness_proxy: 0.43
  max_average_latency_ms: 2500
  max_average_cost_usd: 0.05
  max_quality_regression: 0.03
